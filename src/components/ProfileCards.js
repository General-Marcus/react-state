import React from "react";

export function ProfileCards(props) {
  reture(
    <div className="card">
      <div className="textgroup">
        <h3>{writer.name}</h3>
        <p>{writer.email}</p>
        <p>{writer.email}</p>
        <button className="actionBtn">Read</button>
      </div>
    </div>
  );
}
